{
  "name": "@canopy/deploy",
  "version": "1.0.0",
  "description": "Canopy deploy engine — provision, build, deploy to Hetzner VPS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@canopy/scanner": "1.0.0",
    "ssh2": "^1.16.0"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/ssh2": "^1.15.5"
  }
}
