{
  "name": "canopy",
  "version": "1.1.1",
  "description": "Security scanner + deploy tool for vibecoded apps. Scan for vulnerabilities, deploy to your own VPS with one command.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/scanner && npm run build -w packages/deploy && npm run build -w packages/cli",
    "clean": "rm -rf packages/*/dist"
  },
  "keywords": [
    "security",
    "scanner",
    "deploy",
    "vps",
    "hetzner",
    "docker",
    "cli",
    "vibecoding"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/KenEzekiel/canopy.git"
  },
  "author": "Canopy Contributors",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
